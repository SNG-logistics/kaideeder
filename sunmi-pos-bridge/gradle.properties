org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Set the deployed POS URL and its exact host name before making a release APK.
# Use HTTPS in production. HTTP is allowed only when ALLOW_CLEARTEXT=true.
POS_URL=https://pos.example.com
ALLOWED_HOSTS=pos.example.com
ALLOW_CLEARTEXT=false
