<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      จำเป็นตั้งแต่ Android 11 (API 30) ขึ้นไป
      ระบบซ่อนแอปอื่นจากกันเอง (package visibility) ถ้าไม่ประกาศตรงนี้
      InnerPrinterManager.bindService() จะผูกกับบริการเครื่องพิมพ์ในตัวไม่ติด
      โดยไม่มี error ใด ๆ ผลคือสถานะขึ้น DISCONNECTED ตลอด ทั้งที่เครื่องพิมพ์ปกติดี
      แพ็กเกจนี้ต้องตรงกับ SunmiPrinterManager.PRINTER_SERVICE_PACKAGE
    -->
    <queries>
        <package android:name="woyou.aidlservice.jiuiv5" />
        <intent>
            <action android:name="woyou.aidlservice.jiuiv5.IWoyouService" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="KAIDEEDER POS"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
