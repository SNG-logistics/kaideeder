'use client'
// import { useEffect, useRef } from 'react'
// import { useRouter, usePathname } from 'next/navigation'

export default function AutoLogout() {
    // Disabled AutoLogout per user request to keep sessions active permanently
    return null
}
