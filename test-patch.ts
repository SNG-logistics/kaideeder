import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { const p = await prisma.product.findFirst(); console.log("Product:", p?.id); if(p) { const updated = await prisma.product.update({ where: { id: p.id }, data: { toppingsJson: JSON.stringify([{id:"1", name:"test", price:10}]) } }); console.log("Updated:", updated.toppingsJson); } } main().catch(console.error).finally(()=>prisma.$disconnect());
